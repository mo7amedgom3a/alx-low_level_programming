#include <stdio.h>

/**
 * main -  Entry point
 *
 * discription : Print all the letters except q and e
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for (int i = 48 ; i <= 57 ; i++)
	putchar(i);
	putchar('\n');
	return (0);
}
